"use client";

// ScanFab is no longer used — scan is integrated into the mobile bottom tabs.
// Kept as an empty component to avoid breaking imports during transition.
export default function ScanFab() {
  return null;
}
